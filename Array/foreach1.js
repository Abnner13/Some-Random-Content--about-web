const aprovados = ['Abnner', 'Ana', 'Juliana', 'ikaro']
aprovados.forEach((nome, indece) => {
    console.log(`${indece}: ${nome}`)
})

aprovados.forEach(a => console.log(a))

const exibo = aprovado => console.log(aprovado)
aprovados.forEach(exibouuu)
